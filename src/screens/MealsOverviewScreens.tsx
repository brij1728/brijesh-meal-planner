import { StyleSheet, Text, View } from "react-native";

export const MealsOverviewScreens = () => {
  return (
    <View>
      <Text>MealsOverviewScreens</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },
});
